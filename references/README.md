# References Directory

The reference library is version-controlled and organized to minimize merge conflicts.

- `bibtex/` contains the canonical bibliography.
- `pdf_library/` contains stable-name PDFs when they are legal and practical to store.
- `reading_notes/` contains paper-level notes keyed by citation identifier.

Use dedicated `ref/` branches for bibliography-heavy updates whenever possible.
